Python version used: 3.11.9
[to be continued]

activate venv: `& ./.venv/Scripts/Activate.ps1`
run uvicorn (must cd into app dir first): `uvicorn [the fastapi app name]:app --reload` => for my case is main:app